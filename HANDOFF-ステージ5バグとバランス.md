# 引き継ぎメモ — グラマー・ガード（GrammarGuard）ステージ5バグ＆バランス

記録日: 2026-06-08 / 対象ファイル: `grammar-guard.html` / ブランチ: `claude/stage-5-bugs-balance-dNLNu`

> 家で続きをやるための「記録だけ」のメモ。まだ実装はしていない。実機スクショ2枚を見て整理した内容。

---

## ① ステージ5：敵が城にめり込んで攻撃しない（バグ＋バランス）

### 症状
- ステージ5の耐久戦で、**敵が自城（中央）にめり込んでしまい、攻撃判定が出ない**。
- 加えて、**遠距離攻撃の射程に味方が一瞬で入ってしまって**対処が大変だった（被弾が早すぎる）。

### 設計の方向性（メモ）
中央配置の防衛は当たり判定と射程の両方で破綻しやすい。配置そのものを見直したい。候補：

- **案A（推奨）**: 中央配置はやめる。**城にはダメージが入らない**代わりに、
  **定期的に味方キャラへ固定ダメージを与える攻撃が飛んでくる**中で防衛する形式にする。
- **案B**: 敵城側に**砲台を設置**し、**前衛（味方）へ頻繁にダメージ**を与える形式にする。

→ どちらも「城への直接めり込み攻撃」を排除しつつ、緊張感（固定ダメージ／砲撃）を別ロジックで作るのが狙い。

### 実装メモ（次に着手する人向け）
- ステージ定義・敵の移動／攻撃判定まわりを `grammar-guard.html` 内で確認すること。
- 「めり込んで攻撃しない」は、敵の到達判定（城ヒットボックス）と攻撃トリガの条件が噛み合っていない可能性。
- 案A/B にするなら、敵の近接攻撃ループをやめて**タイマー式の固定ダメージ／砲撃イベント**に置き換える設計が素直。

---

## ② ホーム単語カード：英単語と日本語が縦に切れる（表示バグ）

### 症状
- ホーム画面の単語カードで **`obey` が `obev` に見える**（`y` の下端が切れている）。
- 同じく**日本語（例:「従う」）の下も切れている**。

### 原因（特定済み）
- **データは正しい**。`grammar-guard.html` 5702行に
  `{"...","name":"従う","answer":"obey","desc":"動詞。命令や規則に従う"}` と正常に入っている。
  → タイポではなく**表示側の縦クリッピング**が原因。
- 単語カード `#wordCard` のCSS（297行付近）が
  `height:86px; overflow:hidden;` で**高さ固定＋はみ出し非表示**。
  描画は `renderDailyWord()`（9483行〜）で `answer`→`name`→`desc` の3行を出している（9487-9490行）。
  `fitWord()`（9491行）でフォント調整しているが、`y` の下端や日本語の下が枠＋line-heightで切れている。

### 修正方針（メモ）
- `#wordCard` の `height:86px` 固定をやめて `min-height` にする、もしくは縦パディング／line-heightを増やす。
- 文字の descender（`g `y` `p` 等）と日本語が切れないよう、`fitWord()` の縮小条件と行の `line-height` を合わせて見直す。
- `overflow:hidden` は横の `.wline`（ellipsis）には必要だが、**縦の見切れ**が起きないよう高さ側を緩める。

---

## 参考：該当コードの位置（grammar-guard.html）
| 内容 | 行 |
|---|---|
| `#wordCard` のCSS（height:86px; overflow:hidden） | 297 付近 |
| `.wline`（横はみ出し ellipsis） | 298 |
| 単語カード描画 `renderDailyWord()` | 9483〜9495 |
| `obey` の問題データ（正常） | 5702 |
